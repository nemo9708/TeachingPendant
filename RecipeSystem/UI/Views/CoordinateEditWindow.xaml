<Window x:Class="TeachingPendant.CoordinateEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Coordinate Editor"
        Width="300" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="LightGray">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="R:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtR" Grid.Row="0" Grid.Column="1" Margin="5" Width="100"/>

        <TextBlock Text="θ:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="txtTheta" Grid.Row="1" Grid.Column="1" Margin="5" Width="100"/>

        <TextBlock Text="Z:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="txtZ" Grid.Row="2" Grid.Column="1" Margin="5" Width="100"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="60" Margin="5,0" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="60" Margin="5,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>